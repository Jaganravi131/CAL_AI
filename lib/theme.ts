/**
 * 🎨 BRAND — central theme constants (Polished Light Mode matching Cal AI).
 */

// ── Primary brand color ───────────────────────────────────────────────────────
export const ACCENT = '#000000'           // Black primary brand color

// Derived from ACCENT
export const ACCENT_DIM = 'rgba(0,0,0,0.05)'
export const ACCENT_BORDER = 'rgba(0,0,0,0.09)'
export const ACCENT_GLOW = 'rgba(0,0,0,0.04)'
export const ACCENT_LIGHT = '#2c2c2e'

// ── Backgrounds ───────────────────────────────────────────────────────────────
export const BG = '#f5f5f7'        // Main app background (soft iOS gray)
export const SURFACE = '#ffffff'   // Cards, inputs (pure white)
export const SURFACE2 = '#fafafc'  // Elevated surface
export const SURFACE3 = '#f0f0f4'  // More elevated surface

// ── Text ──────────────────────────────────────────────────────────────────────
export const TEXT_PRIMARY = '#000000'
export const TEXT_SECONDARY = '#6c6c70'
export const TEXT_TERTIARY = '#a1a1a6'
export const TEXT_DISABLED = '#c7c7cc'

// ── Borders ───────────────────────────────────────────────────────────────────
export const BORDER = 'rgba(0,0,0,0.06)'
export const BORDER_ACTIVE = 'rgba(0,0,0,0.15)'

// ── Semantic ──────────────────────────────────────────────────────────────────
export const ERROR = '#ff3b30'      // iOS standard red
export const ERROR_DIM = 'rgba(255,59,48,0.08)'
export const WARNING = '#ff9500'    // iOS standard orange
export const SUCCESS = '#34c759'    // iOS standard green

// ── Tab bar ───────────────────────────────────────────────────────────────────
export const TAB_ACTIVE = '#000000'
export const TAB_INACTIVE = '#8e8e93'
export const TAB_HEIGHT = 68
